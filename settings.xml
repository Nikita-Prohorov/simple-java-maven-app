<settings>
  <mirrors>
    <mirror>
      <!--This sends everything else to /public -->
      <id>NexusRepo</id>
      <mirrorOf>*</mirrorOf>
      <url>http://172.17.0.1:8081/repository/NexusRepo/</url>
    </mirror>
  </mirrors>
  <profiles>
    <profile>
      <id>jenkins</id>
      <!--Enable snapshots for the built in central repo to direct -->
      <!--all requests to nexus via the mirror -->
      <repositories>
        <repository>
          <id>central</id>
          <url>http://central</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>true</enabled></snapshots>
        </repository>
      </repositories>
     <pluginRepositories>
        <pluginRepository>
          <id>central</id>
          <url>http://central</url>
          <releases><enabled>true</enabled></releases>
          <snapshots><enabled>true</enabled></snapshots>
        </pluginRepository>
      </pluginRepositories>
    </profile>
  </profiles>
  <activeProfiles>
    <!--make the profile active all the time -->
    <activeProfile>jenkins</activeProfile>
  </activeProfiles>
</settings>
